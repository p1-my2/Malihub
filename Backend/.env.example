# Copy this file to .env and fill in real values. Never commit the real .env.

DATABASE_URL="mysql://user:password@localhost:3306/malihub"
JWT_SECRET="dev-only-change-this-to-a-long-random-string"
JWT_EXPIRES_IN="7d"
PORT=5000
